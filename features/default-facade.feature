# language: zh-CN
功能: 默认门面全链路回归（P0 修复验收）
  审计报告（docs/bug-audit-report-2026-09-07.md）第六节指出：触发类场景全部绕过接线层
  （注入 shouldCompress 或直调单元函数），P0-1/P0-2/P0-3 的修复成果在默认链路上零覆盖。
  本文件锁定「宿主只提供 compress」这一默认接入方式下的端到端行为：
  编排器内置触发路径、真实轮次状态与默认校验链均不得被占位实现屏蔽。

  背景:
    假如归档使用内存 sink
    并且注入的 callback 只实现了 compress，其余钩子全部缺省
    并且会话为系统提示、两条用户消息与两个完整 tool block
    并且会话容量使占用率高于触发线

  场景: 默认触发路径下压缩不会在首次成功后永久卡死（P0-1 回归）
    当执行完整压缩流程
    那么 第一轮压缩成功提交
    假如 在提交结果上追加新的用户消息与完整 tool block 构造下一轮状态
    当 连续调用两次压缩
    那么 这两次调用均被频率下限抑制而不压缩
    当 继续调用一次压缩
    那么 本轮压缩成功提交
    并且 压缩块总数在原有基础上继续增长
    并且 全程未出现钩子缺失或钩子异常错误

  场景: 压缩后占用回落到目标线以下时不再压缩（P0-2 回归）
    当执行完整压缩流程
    那么 第一轮压缩成功提交
    假如 把提交结果的容量放大使占用率回落到目标线以下
    当执行完整压缩流程
    那么 本轮不压缩且状态逐字节保持原样

  场景: 默认校验链拒绝伪造槽位并走降级链（P0-3 回归）
    假如 compress 钩子返回约束槽位含原文中不存在的取值
    并且 会话改用含重复 hash 指纹的读取输出使 L1 降级输出可被去重缩小
    并且 目标预算压到整段一半以下使目标段走 L2 及以上的 LLM 路径
    当执行完整压缩流程
    那么 被测块最终降级为抽取式
    并且 被测块标记为走了降级链
    并且 伪造的槽位取值不出现在任何压缩块文本中
    并且 有损级别的归档指针仍然有效
    并且 观测告警双通道同步记录了校验重试与降级链告警

  场景: 整体取消通道在提交前放弃本轮（R5-9 回归）
    假如 准备一个已经中止的取消信号
    当带着取消信号执行完整压缩流程
    那么 本轮不压缩且状态逐字节保持原样
    假如 compress 钩子在被调用时触发外部取消并返回合法输出
    当带着取消信号执行完整压缩流程
    那么 提交前被取消：状态逐字节保持原样且不产生观测记录
    并且 compress 钩子收到了已中止的信号

  场景: 布尔触发的决策原因如实标注（R5-8 回归）
    假如 注入恒返回布尔真值的 shouldCompress
    当执行完整压缩流程
    那么 第一轮压缩成功提交
    并且 观测记录的触发原因标注为宿主决策而非任务边界
    并且 观测记录确认头部前缀稳定且断点可置于头部之后

  场景: 降级链的非法 schema 告警进入观测记录（R5-4 回归）
    假如 compress 钩子返回不符合槽位 schema 的输出
    当执行完整压缩流程
    那么 第一轮压缩成功提交
    并且 观测告警包含降级链的非法 schema 记录
